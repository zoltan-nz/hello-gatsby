import React, { FC } from 'react';
import Layout from '../components/layout';
import '../styles/main.scss';

const Home: FC = () => (
  <Layout>
    <h1>Home Page</h1>
  </Layout>
);

export default Home;
